"use client";

import { PatternChatPage } from "../components/shared/PatternChatPage";

export default function Pattern1ChatPage() {
  return (
    <PatternChatPage
      name="Pattern 1: Direct AG-UI"
      description="Frontend connects directly to Python backend via HttpAgent"
      headerColor="bg-blue-50"
    />
  );
}
